`@font-face` is a rule used to specify custom fonts for your document rather than using one of the widely used fonts directly available in the browser.
The font can either be downloaded from an `url` or `local` to download the fonts.
